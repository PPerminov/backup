CREATE TABLE IF NOT EXISTS client (os VARCHAR() NOT NULL,
                                                VERSION VARCHAR(14) NOT NULL,
                                                                    objects TEXT);
